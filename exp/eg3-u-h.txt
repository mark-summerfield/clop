usage: str update [OPTIONS] [TAG]

Updates all the files in the store by creating a new generation and
storing all those that have changed.

POSITIONALS: 
  For the TAG use quotes if multiple words.

OPTIONS:
  -v or --verbose  Show the name of every updated file [default just
                   show counts].
  -h or --help     Show update help and quit.
